# 2026-07-02 工作记录

## 主题切换背景图实时同步修复
- 问题：在"我的"页面切换主题后，背景图不能实时更新
- 修复：在 app.js 增加订阅/取消订阅机制（_themeListeners + onThemeChange/offThemeChange/_notifyThemeChange）
- background-image 组件在 attached 时订阅，detached 时取消订阅，收到通知立刻 refreshBackground()
- mine 页面同样订阅，onUnload 取消，实时更新 isDark 状态
- setThemeMode 和 wx.onThemeChange 回调都会触发 _notifyThemeChange

## 管理端部署配置
- 管理端静态托管网址：https://mvp1-d8grozotx0e93940c-1445854594.tcloudbaseapp.com
- 云函数 HTTP 触发地址：https://mvp1-d8grozotx0e93940c.service.tcloudbase.com/admin-api
- admin-api CORS 兜底值和 config.json 模板均已写入管理端实际域名
- 云控制台还需手动配置 ADMIN_CORS_ORIGIN 和 ADMIN_PASSWORD 环境变量，然后重新部署云函数

## P1 修复开始

### P1-3: 清理根目录44个调试脚本 ✅
- 确认零引用（miniprogram/、cloudfunctions/、app.json 均无引用）
- 全部为 Node.js fs 模块读文件的一次性调试脚本，不影响小程序运行
- 已删除：analyze*.js(13个)、check*.js(14个)、debug_*.js(3个)、find_*.js(6个)、show_*.js(2个)、count_backticks.js、total_bt.js、_check_*.js(2个)

### P1-11: 首页统计区打卡假数据修复 ✅
- 新建 `utils/checkinManager.js`：基于 wx.Storage 的本地打卡追踪
  - 记录每日打卡日期数组，计算连续打卡天数
  - `doCheckin()` 执行打卡，`getCheckinSummary()` 返回 {streak, checkedInToday, totalDays}
- 更新 `pages/index/index.js`：onLoad/onShow 调用 loadCheckinData()，checkinStreak 从 7 改为动态读取
- 更新 `pages/index/index.wxml`："连续打卡"区域绑定 handleCheckinTap，已打卡显示绿色✓
- 更新 `pages/index/index.wxss`：未打卡时显示红点提示，已打卡切换绿色
- 更新 `pages/mine/index.js`：同样接入 checkinManager 替换 checkinStreak: 12
- 未来可平滑升级为云数据库版本（只需替换 checkinManager 内部实现）

### P1-10: 删除死代码（example页面 + cloudTipModal组件）✅
- 确认零外部引用：仅 example 自身引用 cloudTipModal，无任何页面 navigateTo 到 example
- 删除 `miniprogram/pages/example/` 目录（4个文件）
- 删除 `miniprogram/components/cloudTipModal/` 目录（4个文件）
- 从 app.json pages 数组中移除 `pages/example/index`

### 首页统计区死数据全部消除 ✅
修复了统计区的最后两处死数据：

**"已斩知识点"(18) → "学习时长"(每周刷新)：**
- 新建 `utils/studyTimeManager.js`：
  - `addStudyTime(seconds)` — 同时累加当周(周一重置)和总累计时长
  - `getWeeklyFormatted()` / `getTotalFormatted()` — 格式化输出如"3.5小时"
  - `recordQuestionAnswered()` / `getTotalQuestions()` — 累计答题计数
- 在 `pages/practice/index.js` 的 `finishSession()` 中调用 `studyTimeManager.addStudyTime(totalTime)`
- 在 `pages/wrong/review/index.js` 补了 `sessionStartTime`，`finishReview()` 中累加学习时长
- 在两处 `submitAnswer()` 中添加 `studyTimeManager.recordQuestionAnswered()`（刷题+错题复习）

**"待复习"(12) → 错题本真实数据：**
- 首页 index.js 新增 `loadStudyData()`，调用 `wrongBook.getGlobalStats().dueReview`
- `dueReview` 是错题本中 `status='reviewing'` 且 `nextReviewAt <= now` 的错题数

**mine 页面死数据同步修复：**
- `studyTimeText: '22.8小时'` → `studyTimeManager.getTotalFormatted()` 读累计总时长
- `slashCount: 47` → `slashManager.getAllSlashedItems().length`
- `totalQuestions: 1523` → `studyTimeManager.getTotalQuestions()` 读累计答题数

**WXML 修改：**
- index.wxml 统计区："已斩知识点"→"学习时长"（显示 weeklyStudyTime）
- 用户卡片弹窗："已斩知识点"→"本周学习"
- mine 页三项统计均改为实时读取

### 首页历史记录死数据修复 ✅
- 新建 `utils/practiceHistoryManager.js`：
  - `recordSession(info)` 记录/更新一条练习历史（按 bankId 聚合，同题库多次刷题合并）
  - `getHistory()` / `getRecent(n)` 读取历史列表
  - `formatRelativeTime()` 格式化"刚刚/X分钟前/昨天/X天前"
  - 存储 key: `practice_history`，最多保留 50 条
- `pages/practice/index.js` finishSession 调用 recordSession，session 增加 bankId/bankCategory 字段
- `pages/memorize/index.js` finishMemorizeSession 同样记录学习时长+历史
- `pages/bank/list/index.js` 和 `pages/exam/list/index.js` 存 practiceSession 时增加 bankCategory 字段
- `pages/index/index.js`：
  - 删除硬编码的 3 条 historyBanks 假数据
  - 新增 `loadHistoryData()` 从 practiceHistoryManager.getRecent(3) 读取
  - onLoad/onShow 都调用 loadHistoryData
  - handleHistoryTap 改为跳转题库列表页（switchTab）
  - WXML 增加空状态展示"还没有练习记录"
- `pages/history/index.js`：
  - 删除 mockData，改从 practiceHistoryManager.getHistory() 读取真实数据
  - WXML 字段适配（lastTime、accuracy、已练 X 题）
  - 空状态按钮改为跳转题库列表页

### P1-9: 隐私API授权检查 ✅
- 新建 `utils/privacyAuth.js`：
  - `ensureAuthorized()` 调用 wx.getPrivacySetting 检查授权状态
  - 未授权时调用 wx.requirePrivacyAuthorize 弹出微信原生隐私授权窗
  - 用户拒绝时弹窗引导前往设置查看隐私协议
  - 旧版本基础库（无 getPrivacySetting）降级放行
- 5处调用点全部加上授权检查：
  - `utils/imageUploader.js` chooseImages() — 选图（相册/拍照）
  - `pages/bank/import/index.js` chooseExcel() — 选Excel文件
  - `pages/bank/import/index.js` chooseWord() — 选Word文档
  - `pages/bank/import/index.js` takePhoto() — OCR拍照
  - `pages/bank/import/index.js` chooseImage() — OCR选相册图
- app.json `__usePrivacyCheck__: true` 早已配置，配合隐私协议即可生效

### P1-8: 管理端错误消息转义 ✅
- `管理端网页/js/app.js` 6处 `res.msg` 拼接全部改为 `escHtml(res.msg)`：
  - 第161行 renderDashboard 加载失败
  - 第233行 renderExamCategories 加载失败
  - 第308行 renderBanks 加载失败
  - 第646行 renderQuestions 加载失败
  - 第1062行 renderNews 加载失败
  - 第1288行 renderFeedback 加载失败
- escHtml() 函数早已存在（第1388行），转义 & < > " ' 五个字符
- 之前的 newsParseStatus 已使用 escHtml（第1166/1171行），无需重复修复

### P1-4: 云函数调试日志清理 ✅
- `cloudfunctions/quickstartFunctions/index.js` 移除10处过程性 console.log：
  - parseWordDocument 函数内的 Started/Downloading/File downloaded/mammoth loaded/Extracting/Text extracted/Calling parseOcrText/parseOcrText returned/Fallback returned/Question type distribution
- 保留全部 console.error（错误日志）和 console.warn（降级提示）
- 清理后云函数日志只剩关键错误和降级告警，便于线上排查

### P1-1: 基础库版本升级 ✅
- project.config.json libVersion 从 2.20.1 升级到 3.x
- 用户最初选了 3.16.1（2025最新版）出现 timeout 报错，建议降到 3.5.5 稳定版
- 3.5.5 完整支持 wx.getPrivacySetting 隐私API，满足合规要求

### P1-6: importBank 改批量并发写入 ✅
- `cloudfunctions/quickstartFunctions/index.js` importBank 函数：
  - 之前：for 循环逐条 `await db.collection('questions').add()`，500题串行写入约30秒
  - 之后：每批20条用 `Promise.all` 并发写入，500题仅需25批，预计3-5秒
- 微信云数据库单次 add 最多20条，所以 BATCH_SIZE=20 是最优值
- Excel/Word 导入复用 importBank，同步提速
