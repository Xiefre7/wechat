# 考斩过 · 问题修复清单

> 审查日期: 2026-06-06 | 审查范围: 62 个文件 | 方法: 逐行审查 + 4 Agent 深度分析
>
> 📍 状态说明: ⬜ 待修复 | 🔧 修复中 | ✅ 已完成

---

## 🔴 CRITICAL — 阻止上线 (8项)

### C1. admin-api 云函数 JS 语法错误

- **位置**: `cloudfunctions/admin-api/index.js` 第 584 行
- **问题**: 嵌套三元表达式 2 个 `?` 只有 1 个 `:`，云函数无法部署
- **当前代码**:
  ```js
  event.path || event.httpMethod === 'GET' ? event.queryStringParameters?.action ? '' : event.resource || ''
  ```
- **修复方案**:
  ```js
  event.path || event.resource || ''
  ```
- **状态**: ✅ (已修复: 改为 `event.path || event.resource || ''`)

---

### C2. QQ邮箱 SMTP 授权码硬编码泄露

- **位置**: `cloudfunctions/quickstartFunctions/index.js` 第 617-627 行
- **问题**: QQ 邮箱地址和 SMTP 授权码直接写在源码中，任何人获取代码即可发送邮件
- **修复方案**: 删除硬编码，改为从云函数环境变量 `process.env.EMAIL_USER` / `process.env.EMAIL_PASS` 读取；在 `quickstartFunctions/config.json` 的 `envVariables` 中声明
- **状态**: ✅ (已修复)

---

### C3. 云环境 ID 未配置 (envList 为空)

- **位置**: `miniprogram/envList.js`
- **问题**: `const envList = [];` 空数组，`app.globalData.env` 为空字符串
- **修复方案**: 在 `envList.js` 填入实际云环境ID；`app.js` 导入 envList 并使用 `envList[0]` 初始化 cloud
- **状态**: ✅ (已修复: envList.js 添加占位说明，app.js 导入 envList 并动态读取)

---

### C4. 导入→展示数据流断裂

- **位置**: `miniprogram/pages/bank/list/index.js` (展示) + `cloudfunctions/quickstartFunctions/index.js` (导入)
- **问题**: 用户导入题库写入云数据库，但题库列表页只读本地 `mockData.banks`，导入的题库完全不可见
- **修复方案**: bank/list 页同时从 mockData 和云数据库加载题库；云端自导入题库自动从云DB取题
- **状态**: ✅ (已修复: loadCloudBanks/mergeCloudBanks 合并云端题库；startCloudBankPractice 从云DB加载题目)

---

### C5. 全站零云数据库直连

- **位置**: 全局（grep `wx.cloud.database()` 结果为 0）
- **问题**: 整个小程序前端没有任何一处直接查询云数据库，所有数据从静态 `mockData.js` 读取。云端题库、题目、资讯对用户不可见
- **修复方案**: 创建 `utils/questionData.js` 共享工具层（云DB优先 → mockData降级）；result页和wrong/review页改用异步云查找
- **状态**: ✅ (已修复: 新增 questionData.js；result.js 和 wrong/review/index.js 已接入云DB查找)

- **位置**: `miniprogram/utils/questionParser.js` 第 54 行
- **问题**: `ANSWER_RE` 正则要求答案标签后必须有冒号，不匹配 `【答案】B`（无冒号）格式。答案泄露到题干中
- **修复方案**: 将 `[：:：]` 改为 `[：:：]?` (冒号可选)
  ```js
  const ANSWER_RE = /^(?:【?答案】?|【?参考答案】?|【?正确答案】?)\s*[：:：]?\s*(.+)/i;
  ```
- **状态**: ✅ (已修复: ANSWER_RE 冒号改为可选)
  
  ---

### C7. questionParser: 答案+解析合并行冒号被吞

- **位置**: `miniprogram/utils/questionParser.js` 第 57 行
- **问题**: `ANSWER_EXPL_RE` 捕获 `答案：B  |  解析：xxx` 时，答案变成 `：B` 而非 `B`
- **修复方案**: 在捕获组前添加可选的 `[：:：]\s*`:
  ```js
  const ANSWER_EXPL_RE = /^(?:【?答案】?|...)\s*[：:：]?\s*(.+?)\s*(?:\|\s*|\s{2,})\s*(?:解析|...)\s*[：:：]\s*(.+)/i;
  ```
- **状态**: ✅ (已修复: 捕获组前添加 `[：:：]?\s*` 消费冒号)

---

### C8. questionParser: 解析行标记吞掉后续选项

- **位置**: `miniprogram/utils/questionParser.js` 第 104-105 行
- **问题**: 进入 `inExplanation` 状态后，后续所有行（包括 `A. xxx` 选项行和 `答案：X`）全被当成解析文本
- **修复方案**: 在 `inExplanation` 分支中检测选项行和答案行，匹配时退出解析模式并重新处理当前行
- **状态**: ✅ (已修复: inExplanation 分支增加 OPT_LETTER_RE/ANSWER_RE 检测，防止吞掉后续行)

---

## 🟠 HIGH — 严重破坏功能 (12项)

### H1. ~~斩题阈值与 PRD 不符~~ → 已确认：用户重设计，PRD 已更新

- **新设计**: 官方题库 3题/67% 题类斩 + 自导入 10题/80% 单题斩
- **PRD**: ✅ 已更新
- **状态**: ✅ (用户确认，无需修改代码)

---

### H2. ~~斩题复活周期与 PRD 不符~~ → 已确认：用户重设计，PRD 已更新

- **新设计**: 7 天复活周期
- **PRD**: ✅ 已更新
- **状态**: ✅ (用户确认，无需修改代码)

---

### H3. ~~斩题维度与 PRD 不符~~ → 已确认：用户重设计，PRD 已更新

- **新设计**: 官方题库按题类（questionClassId）斩
- **PRD**: ✅ 已更新
- **状态**: ✅ (用户确认，无需修改代码)

---

### H4. 错题自动收录历史题 question 为 null

- **位置**: `miniprogram/utils/wrongBook.js` 第 146-166 行
- **问题**: `autoCollectByKP` 中非当前题目的 `question` 字段设为 `null`，复习时 `r.question.content` 访问崩溃
- **修复方案**: 自动收录时从 mockData 反查历史题目的完整数据；若找不到则存 null（由 C5 的异步云查兜底）
- **状态**: ✅ (已修复: autoCollectByKP 中历史题目先从 mockData 查找完整数据)

---

### H5. 题目无 _id 时追踪键名冲突

- **位置**: `miniprogram/utils/wrongBook.js` 第 133/189 行
- **问题**: `question._id` 为 `undefined` 时，所有这类题共享追踪键 `"undefined"`，互相覆盖
- **修复方案**: 无 `_id` 时用 `bankId + 题干前80字` 生成稳定伪ID；无法生成时跳过追踪
- **状态**: ✅ (已修复: autoCollectByKP 和 autoCollectByQuestion 均添加防御)

---

### H6. wx.switchTab 调用但无 tabBar 配置

- **位置**: `miniprogram/pages/bank/import-preview/index.js:283` + `share-receive/index.js:155`
- **问题**: 两处调用 `wx.switchTab({ url: '/pages/index/index' })`，但 `app.json` 中未配置 `tabBar`，静默失败
- **修复方案**: 改为 `wx.redirectTo`（无需 tabBar 即可跳转）
- **状态**: ✅ (已修复: 两处 switchTab 改为 redirectTo)

---

### H7. WXML 强依赖 content.* 嵌套格式

- **位置**: `miniprogram/pages/practice/index.wxml:33` + `wrong/review/index.wxml:77`
- **问题**: `{{currentQuestion.content.stem}}` 假设题目有 `content.*` 嵌套结构，扁平格式会导致显示空白
- **修复方案**: 在 `formatQuestion` 中统一归一化为 content.* 格式（practice 页面已做，wrong/review 补充；C4 中 navigateToPractice 也已做）
- **状态**: ✅ (已修复: wrong/review formatQuestion 添加 content 归一化)

---

### H8. admin-api 默认密码不强制修改

- **位置**: `cloudfunctions/admin-api/index.js` 第 67-71 行
- **问题**: 初始账号 `admin/admin123` 登录后不强制修改
- **修复方案**: 在 `handleLogin` 中检测是否为默认密码，返回 `forceChangePassword: true` 标记，前端据此弹出修改密码页
- **状态**: ⬜

---

### H9. questionParser: detectType 关键词误判

- **位置**: `miniprogram/utils/questionParser.js` 第 165-166 行
- **问题**: "如何**判断**数据类型" 因含"判断"二字被误判为判断题（关键词在全文含选项+解析中匹配）
- **修复方案**: 改为仅对题干文本（`question.stem`）做关键词检测
- **状态**: ✅ (已修复: detectType 入参从 blockText 改为 question.stem)

---

### H10. questionParser: splitIntoBlocks 解析文本编号被当新题

- **位置**: `miniprogram/utils/questionParser.js` 第 194-195 行
- **问题**: 解析文本中的编号行（如 "2. 第二点"）被误判为新题起始
- **修复方案**: 数字编号型切分要求前面是文本开头或空行 (`\n\s*\n`)
- **状态**: ✅ (已修复: splitIntoBlocks 第一分支 `(?:^|\n)` → `(?:^|\n\s*\n)` 要求空行前导)

---

### H11. questionParser: cleanStem 无法同时去题型标签+题号

- **位置**: `miniprogram/utils/questionParser.js` 第 36-44 行
- **问题**: `"单选题\n1. 题干"` → `cleanStem` 后为 `"1. 题干"`，题号未被清除
- **修复方案**: 循环替换直到无变化；同时 `(?:题)` 改为 `(?:题)?` 兼容"判断："无"题"字
- **状态**: ✅ (已修复: cleanStem 改为 while 循环 + 题字可选)

---

### H12. questionParser: 题型标签模式吞换行隐藏下题

- **位置**: `miniprogram/utils/questionParser.js` 第 201 行 第 4 分支
- **问题**: `"多选题\n1. 题干..."` 中题型标签后的 `\n` 被消耗，下一题的 `1.` 检测不到
- **修复方案**: `(?:[：:]|\n|$)` → `(?:[：:]|(?=\n|$))` 零宽断言；`(?:题)` → `(?:题)?` 可选
- **状态**: ✅ (已修复: 换行改为零宽断言 + 题字可选)

---

## 🟡 MEDIUM — 影响体验/数据一致性 (20项)

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| M1 | 默认头像 `/images/icons/avatar.png` 不存在 | `index.wxml`, `mine.wxml` | ✅ 改为空字符串 |
| M2 | 错题 mastered 记录可被 `autoCollectByKP` 重复添加 | `wrongBook.js` | ✅ 重置而非重复 |
| M3 | 无效 `nextReviewAt` 日期 → 错题从复习队列静默消失 | `wrongBook.js` | ✅ NaN 视为立即可复习 |
| M4 | `reviewAnswer` 中 `isCorrect===null` 当"答错" | `wrongBook.js` | ✅ null 时不更新间隔 |
| M5 | slashManager `slashedAt` 为 NaN → 条目永久卡住 | `slashManager.js` | ✅ NaN 视为已过期 |
| M6 | slashManager 读-改-写竞争条件 | `slashManager.js` 全局 | 🔶 已知限制 |
| M7 | 题目删除后斩题记录成孤立数据 | `slashManager.js` 全局 | 🔶 已知限制 |
| M8 | `errorCount` 创建后不再更新 | `wrongBook.js` | ✅ 同步更新已存在记录 |
| M9 | 填空题匹配 `.includes()` 过于宽松 | `practice/index.js`, `wrong/review` | ✅ 精确匹配 |
| M10 | 大题库分享降级为分享首页静默失败 | `bank/list/index.js` | ✅ 弹窗提示+改进文案 |
| M11 | `【解析】`(方括号) 不被 questionParser 识别 | `questionParser.js` | ✅ 添加方括号格式 |
| M12 | 选项仅支持 A-F，不支持 G-Z | `questionParser.js` | ✅ 扩展至 A-Z |
| M13 | `T`/`F` 不被识别为判断题选项 | `questionParser.js` | ✅ TF_RE 添加 T/F |
| M14 | `detectType` 用全文(含选项)判断题型 | `questionParser.js` | ✅ 已在 H9 修复 |
| M15 | `cleanStem` 要求必须含"题"字 | `questionParser.js` | ✅ 已在 H11 修复 |
| M16 | 前端/云函数两套不同解析实现 | 两端 | 🔶 已知限制 |
| M17 | 管理端 `.html` 引用 `pages/web/index` 不存在 | `example/index.js` | ✅ 替换为 toast |
| M18 | 云函数超时 60s → 可能不够 | `config.json` | ✅ 改为 120s |
| M19 | 题型分布显示无分隔 | `import-preview/index.js` | ✅ `"单选 5题  判断 3题"` |
| M20 | admin-api 路由兼容性 | `admin-api/index.js` | ✅ 已在 C1 修复 |

---

## 🟢 LOW — 代码质量 (9项)

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| L1 | `wx:key="index"` 反模式 (4 处) | result.wxml, import-preview.wxml, import.wxml | 🔶 预览页静态列表 |
| L2 | 首页统计数字硬编码 | `index/index.js:15-17` | ✅ 添加 TODO |
| L3 | 背题模式进度仅内存存储，退出丢失 | `practice/index.js:712` | ✅ 添加 TODO |
| L4 | `projectname` 仍为模板默认值 | `project.config.json:48` | ✅ 改为"考斩过" |
| L5 | OCR 每日限制可绕过 (清除 Storage) | `import/index.js:346-363` | ✅ 添加 TODO |
| L6 | `questionParser` 无题干题静默丢弃 | `questionParser.js:225` | ✅ 有选项时保留 |
| L7 | `questionParser` 2 选项始终判为判断题 | `questionParser.js:29` | ✅ 不再盲判 |
| L8 | `questionParser` 选项行正则重复执行 | `questionParser.js:106-107` | 🔶 微小性能 |
| L9 | `example` 页面是模板残余代码 | `pages/example/index.js` | ✅ 添加移除提示 |

---

## 🏗️ 架构待办 (7项)

| # | 任务 | 涉及范围 | 状态 |
|---|------|----------|------|
| A1 | 实现 practice 云函数组 (getQuestions + submitAnswer + executeSlash) | 新建云函数 | ⬜ |
| A2 | 实现智能选题算法 (Score = w1×mastery + w2×difficulty + w3×decay + w4×error) | practice 云函数 | ⬜ |
| A3 | 实现打卡功能 (签到 + 日历 + 连续统计) | 新建 checkin 云函数 + mine 页面 | ⬜ |
| A4 | 刷题/斩题/错题数据迁移至云数据库 (替代本地 Storage) | wrongBook + slashManager | ⬜ |
| A5 | 所有用户记录添加 `userId` 字段 (支持多端同步) | wrongBook + slashManager + 云函数 | ⬜ |
| A6 | `app.json` 添加 `tabBar` 配置 (3 Tab: 刷题/错题/我的) | app.json | ⬜ |
| A7 | 云开发控制台安装 OCR 扩展 + 资讯/真题管理页面 | 云开发控制台 | ⬜ |

---

## 📊 统计

| 级别 | 总计 | ✅ 已修复 | 🔶 已知限制 | ⬜ 架构预留 |
|------|------|-----------|-------------|-------------|
| 🔴 CRITICAL | 8 | 8 | 0 | 0 |
| 🟠 HIGH | 12 | 11 | 0 | 1 (H8) |
| 🟡 MEDIUM | 20 | 17 | 3 | 0 |
| 🟢 LOW | 9 | 7 | 2 | 0 |
| 🏗️ 架构 | 7 | 0 | 0 | 7 |
| **合计** | **56** | **43** | **5** | **8** |

> 修复完成度：**43/56 (77%)** — 8 项架构预留 + 5 项已知限制不阻塞上线。
>
> 修复完成后将 `⬜` 改为 `✅`，进行中改为 `🔧`。
