<image
  class="bg-image {{loading ? 'bg-image-loading' : ''}}"
  src="{{imagePath}}"
  mode="aspectFill"
  style="opacity: {{opacity}};"
  bindload="onImageLoad"
  binderror="onImageError"
/>
<view class="bg-fallback {{overlayClass}}"></view>
<view class="bg-overlay {{overlayClass}}"></view>
